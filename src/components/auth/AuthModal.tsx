// Canonical authentication surface lives at src/components/AuthModal.tsx.
// Keep this compatibility export so legacy imports cannot create a second
// registration/login implementation.
export { default, AuthModal } from '../AuthModal';
